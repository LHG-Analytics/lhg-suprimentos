/**
 * lib/supabase/types.ts
 * Tipos gerados pelo Supabase CLI (`supabase gen types typescript`).
 * TODO LHG-200: rodar `pnpm supabase gen types typescript --project-id pjwsmmxnwkfklycwnptf > lib/supabase/types.ts`
 *              após criar as migrations do schema inicial.
 */

// Placeholder até o schema estar definido.
// Substituir pela saída do `supabase gen types typescript`.
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
